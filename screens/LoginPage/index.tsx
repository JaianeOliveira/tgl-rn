import { View, Text } from "react-native";
import React from "react";

import { Screen, PageTitle } from "./styles";

const LoginPage = () => {
  return (
    <Screen>
      <PageTitle>Authentication</PageTitle>
    </Screen>
  );
};

export default LoginPage;
