import { View, Text } from "react-native";
import React from "react";

import { useSelector } from "react-redux";

const GameButtonGroup = () => {
  return (
    <View>
      <Text>GameButtonGroup</Text>
    </View>
  );
};

export default GameButtonGroup;
